{% include 'default/_form.html.twig' with {
    page_title: 'title_vote_add',
    page_title_param: {'%id%': vote.book.id},
    attr: {
        method: 'POST',
        action: url('add_vote', {'id': vote.book.id})
    },
    form: form,
    submit_label: 'action_vote'|trans,
    back_to_list_path: 'book_index',
} %}