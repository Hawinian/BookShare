{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_book_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_book_list'|trans }}</h1>
    {% if pagination is defined and pagination.items|length %}

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label_id'|trans, 'book.id') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label_vote_id'|trans, 'avg_rate') }}</th>

            </tr>
            </thead>
            <tbody>
            {% for book in pagination.items %}
                {% dump(book) %}
                <tr>
                    <td>{{ book.id }}</td>
{#                    {% if book.votes|length != 0 %}#}
{#                    {% set sumratings = 0 %}#}
{#                    {% for vote in book.votes %}#}
{#                        {% set sumratings = sumratings + vote.rate %}#}
{#                        {% endfor %}#}
{#                   <td>{{ sumratings / book.votes|length }}</td>#}
{#                    {% else %}#}
{#                       <td> {{ 'empty' }} </td>#}
{#                        {% endif %}#}
{#                    {% if (avg_rate === null) %}#}
{#                    <td>{{ 0 }}</td>#}
{#                    {% else %}#}
                        <td>{{ book.avg_rate }}</td>
{#                        {% endif %}#}

                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>


    {% else %}
        <p>
            {{ 'message_empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}