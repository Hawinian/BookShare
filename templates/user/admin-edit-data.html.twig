{% include 'default/_form.html.twig' with {
    page_title: 'title_admin_edit_data'| trans,
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('admin_edit_data', {'id': user.id}),
    },
    form: form,
    back_to_list_path: 'user_index',
} %}