{% include 'default/_form.html.twig' with {
    page_title: 'title_petition_kind_create',
    attr: {
        method: 'POST',
        action: url('petition_kind_create')
    },
    form: form,
    submit_label: 'action_create'|trans,
    back_to_list_path: 'petition_kind_index',
} %}