{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_petition_kind_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_petition_kind_list'|trans }}</h1>
    <a class="btn btn-primary" href="{{ url('petition_kind_create') }}" role="button">{{ 'action_create_petition_kind'|trans }}</a>
    <br><br>
    {% if pagination is defined and pagination.items|length %}

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label_id'|trans, 'petition_kind.id') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label_name'|trans, 'petition_kind.name') }}</th>
                <th>{{ 'label_edition'|trans }}</th>
                <th>{{ 'label_deletion'|trans }}</th>

            </tr>
            </thead>
            <tbody>
            {% for petition_kind in pagination.items %}
                <tr>
                    <td>{{ petition_kind.id }}</td>
                    <td>{{ petition_kind.name }}</td>
                    <td>
                        <a href="{{ url('petition_kind_edit', {id: petition_kind.id}) }}" title="{{ 'action_edit'|trans }}">
                            {{ 'action_edit'|trans }}
                        </a>
                    </td>
                    <td>
                        <a href="{{ url('petition_kind_delete', {id: petition_kind.id}) }}" title="{{ 'action_delete'|trans }}">
                            {{ 'action_delete'|trans }}
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

    {% else %}
        <p>
            {{ 'message_empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}