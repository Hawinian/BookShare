{% include 'default/_form.html.twig' with {
    page_title: 'title_status_delete'| trans,
    page_title_param: {'%id%': status.id},
    attr: {
        method: 'DELETE',
        action: url('status_delete', {'id': status.id}),
    },
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'status_index',
} %}